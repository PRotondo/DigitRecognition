package ReadData;

abstract public class TestCase {
	/* returns the input data with the addition of the bias +1 at index 0 */
	public double [] getTestData()
	{
		return null;
	}
	/* Valid labels for classification are >= 0 */
	public int getTestLabel()
	{
		return -1;
	}
}
